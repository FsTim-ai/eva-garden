# Base URL of the sensor backend (see backend/README.md), used for the
# "Excel Log" download button. Defaults to the deployed Railway instance
# in src/components/Dashboard.tsx if left unset.
VITE_BACKEND_URL=
